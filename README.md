# WorkPartsFrontEnd

Project with webpack-dev-server

## Install yarn
Run `npm install --global yarn`

## Install dependencies

Run `yarn install`

## Build app

Run `yarn build`

## Run app

Run `yarn run-app`

## Running unit tests

Run `yarn test`